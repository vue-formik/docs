<script setup lang="ts"></script>

<template>
  <section class="use-formik-view">
    <h1>UseFormik Composable</h1>
    <p>
      The <code>useFormik</code> composable is a utility function that simplifies the process of
      working with forms in Vue 3 applications. It is designed to work with the
      <code>Formik</code>{' '} library.
    </p>
    <p>
      The <code>useFormik</code> composable provides a set of functions and properties that can be
      used to manage form state, validate form data, and handle form submission.
    </p>
    <p>The <code>useFormik</code> composable is used as follows:</p>
    <pre><code>
import { useFormik } from 'vue-formik';
    </code></pre>
    <p>
      The <code>useFormik</code> composable accepts an options object as an argument. The options
      object can contain the following properties:
    </p>
  </section>
</template>

<style scoped lang="sass"></style>
