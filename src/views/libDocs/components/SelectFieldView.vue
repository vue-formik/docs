<script setup lang="ts"></script>

<template>
  <div>
    <h1>Select Field</h1>
    <p>
      The <code>SelectFieldView</code> component is a simple select field that can be used in forms.
    </p>
    <p>It is a wrapper around the native <code>select</code> element.</p>
    <p>It accepts all the props that a native <code>select</code> element accepts.</p>
    <p>
      It also accepts a <code>label</code> prop which is used to display a label for the select
      field.
    </p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the select field.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the select field.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the select field.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the select field.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the select field.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the select field.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the select field.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the select field.</p>
    <p>The <code>label</code> prop is optional.</p>
  </div>
</template>

<style scoped lang="sass"></style>
