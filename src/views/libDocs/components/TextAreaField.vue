<script setup lang="ts"></script>

<template>
  <div>
    <h1>Textarea Field</h1>
    <p>
      The <code>TextareaField</code> component is a simple textarea field that can be used in forms.
    </p>
    <p>It is a wrapper around the native <code>textarea</code> element.</p>
    <p>It accepts all the props that a native <code>textarea</code> element accepts.</p>
    <p>
      It also accepts a <code>label</code> prop which is used to display a label for the textarea.
    </p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the textarea.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the textarea.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the textarea.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the textarea.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the textarea.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the textarea.</p>
    <p>The <code>label</code> prop is optional.</p>
    <p>The <code>label</code> prop is used to display a label for the textarea.</p>
    <p>The <code>label</code> prop is optional.</p>
  </div>
</template>

<style scoped lang="sass"></style>
